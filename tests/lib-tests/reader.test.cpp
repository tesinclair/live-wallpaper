#include "reader.test.h"

void testReader(int silent){
    if (silent){
        return;
    }
}

// Ensures the reader is created and initialised properly
void testContruct(char* testfile){
}

// this is obvious
void testGetVideoData(char* testfile){
}

// this is obvious
void testGetCurrentFrame(char* testfile){
}

// ensures the file is correctly closed
void testDestruct(char* testfile){
}
